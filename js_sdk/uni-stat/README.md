# js_sdk/uni-stat/

统计工具 SDK。提供统计数据查询构建、格式化、图表数据处理等辅助函数，供 `pages/uni-stat/` 下的统计页面使用。

## 文件

| 文件 | 职责 |
|---|---|
| `util.js` | 通用工具 — 日期处理、数据聚合、查询条件构建 |
| `timeUtil.js` | 时间工具 — 时间范围计算、格式化、相对时间 |

## 使用场景

统计页面通过 `import` 引入这些工具，构建 JQL 查询条件、格式化统计结果、生成图表数据。
